# ackel: a grep-like program inspired by ack

wish list:

- parallelism
- rule based search mechanism
- preprocessing (transform files prior to search)
- extendible
- can run on posix platforms + windows
- support `find` attributes
- support pcre
